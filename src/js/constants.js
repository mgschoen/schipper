export default {
    animation: {
        initialZoom: 17.5,
        initialMarkerSize: 10,
        zoomDuration: 3000,
        zoomFeatureThresholdMin: 10,
        zoomFeatureThresholdMax: 75,
        zoomStep: 0.5
    }
}
